// Zona PROFESIONAL — requiere requiereProfesional() en el server component/action
export default function DashboardPage() {
  // TODO: resumen — próximas citas, tests pendientes de revisar,
  // consentimientos pendientes de firma
  return <div>Panel profesional</div>;
}
